# Quarkus configuration
quarkus.http.port=8082

# MCP Server configuration
# Use base URL (StreamableHttpMcpTransport handles the rest)
mcp.server.url=http://localhost:8480/mcp

# Optional Bearer token for MCP server authentication
# Leave empty if no auth is needed
mcp.server.bearer-token=
